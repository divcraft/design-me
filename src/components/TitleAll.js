import React from 'react';

const TitleAll = ({ title }) => {
   return (
      <div className='title-all'>
         <h1 className='wrapper'>{title}</h1>
      </div>
   );
}

export default TitleAll;